{% if items %}
  <nav{{ attributes.addClass(['menubar', 'is-uppercase']) }}>
    <ul>
    {% for item in items %}
      <li>{{ link(item.title, item.url) }}</li>
    {% endfor %}
    </ul>
  </nav>
{% endif %}